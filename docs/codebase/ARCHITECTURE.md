# Architecture

## Core Sections (Required)

### 1) Architectural Style

- Primary style: [TODO] No application architecture is implemented.
- Why this classification: The repository contains no runtime source, manifest, or build configuration.
- Primary constraints: The user has stated an intended CLI that generates Vue projects using shadcn-vue and Tailwind CSS; this is a product requirement, not current code evidence.

### 2) System Flow

```text
[TODO: CLI entry] -> [TODO: prompt/config] -> [TODO: template selection] -> [TODO: file generation] -> [TODO: generated project]
```

The flow is not implemented in the current repository.

### 3) Layer/Module Responsibilities

| Layer or module | Owns | Must not own | Evidence |
|-----------------|------|--------------|----------|
| [TODO] | [TODO] | [TODO] | No source files exist |

### 4) Reused Patterns

No repeated implementation patterns can be verified in the current repository.

### 5) Known Architectural Risks

- The target CLI architecture, template versioning strategy, and generated-project contract are not implemented; changes cannot yet be validated against code.

### 6) Evidence

- `D:\client\fluffy-design-pro\.gitignore`
- Top-level repository listing
- `D:\client\fluffy-design-pro\docs\overview\design.md` (target design, once created; not implementation evidence)
