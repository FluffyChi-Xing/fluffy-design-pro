<script setup lang="ts">
import { shallowRef } from 'vue'
const dialogOpen = shallowRef(false)
</script>
<template><section class="page"><header><p class="eyebrow">{{ $t('showcase.eyebrow') }}</p><h1>{{ $t('showcase.componentsTitle') }}</h1><p>{{ $t('showcase.componentsDescription') }}</p></header><div class="grid"><article class="panel"><h2>{{ $t('showcase.actions') }}</h2><div class="actions"><button class="primary">{{ $t('showcase.primary') }}</button><button class="secondary">{{ $t('showcase.secondary') }}</button><button class="ghost" @click="dialogOpen=true">{{ $t('showcase.dialog') }}</button></div></article><article class="panel"><h2>{{ $t('showcase.states') }}</h2><div class="badges"><span class="success">{{ $t('common.ready') }}</span><span class="warning">{{ $t('common.review') }}</span><span class="danger">{{ $t('common.failed') }}</span></div></article></div><article class="panel"><h2>{{ $t('showcase.empty') }}</h2><div class="empty"><strong>—</strong><p>{{ $t('showcase.emptyDescription') }}</p></div></article><div v-if="dialogOpen" class="overlay" @click.self="dialogOpen=false"><section class="dialog"><h2>{{ $t('showcase.dialogTitle') }}</h2><p>{{ $t('showcase.dialogDescription') }}</p><button class="primary" @click="dialogOpen=false">{{ $t('common.close') }}</button></section></div></section></template>
<style scoped>
.page{display:grid;gap:18px}.eyebrow{color:var(--primary);font-size:11px;font-weight:750;letter-spacing:.08em;margin:0 0 10px;text-transform:uppercase}h1{font-size:clamp(1.8rem,3vw,2.5rem);letter-spacing:-.045em;margin:0}header p:not(.eyebrow),.dialog p{color:var(--muted-foreground);font-size:14px;margin:10px 0 0}.grid{display:grid;gap:14px;grid-template-columns:1fr 1fr}.panel{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);box-shadow:var(--shadow-sm);padding:20px}.panel h2,.dialog h2{font-size:14px;margin:0}.actions,.badges{display:flex;flex-wrap:wrap;gap:9px;margin-top:18px}button{border-radius:var(--radius-sm);cursor:pointer;font-size:13px;font-weight:700;padding:9px 12px}.primary{background:var(--primary);border:0;color:var(--primary-foreground)}.secondary{background:var(--surface);border:1px solid var(--border);color:var(--foreground)}.ghost{background:transparent;border:0;color:var(--primary)}.badges span{border-radius:999px;font-size:11px;font-weight:700;padding:5px 8px}.success{background:color-mix(in srgb,var(--success) 13%,transparent);color:var(--success)}.warning{background:color-mix(in srgb,var(--warning) 13%,transparent);color:var(--warning)}.danger{background:color-mix(in srgb,var(--danger) 13%,transparent);color:var(--danger)}.empty{color:var(--muted-foreground);padding:28px 0;text-align:center}.empty strong{color:var(--subtle-foreground);font-size:28px}.empty p{font-size:13px}.overlay{align-items:center;background:oklch(0.1 0.01 260 / .45);display:flex;inset:0;justify-content:center;position:fixed;z-index:50}.dialog{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);box-shadow:var(--shadow-md);max-width:390px;padding:22px;width:calc(100% - 32px)}.dialog button{margin-top:20px}@media(max-width:600px){.grid{grid-template-columns:1fr}}
</style>