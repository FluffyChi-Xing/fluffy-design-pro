export const deferredCommands = {
  adopt: 'The adopt command will be available after the initial create workflow is stable.',
  migrate: 'The migrate command will be available after the initial create workflow is stable.'
} as const
